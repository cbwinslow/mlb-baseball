## 1. `arm_slot_engine`

- [x] 1.1 Add a real-data query helper (e.g. `arm_slot_from_statcast(pitcher_id, date_from, date_to, conn)`) that reads `AVG(arm_angle)`/`STDDEV(arm_angle)` from `raw.statcast_pitch` for a pitcher + date range, filtered to non-null `arm_angle` rows, and returns `None`/an explicit "no data" result when zero rows match. Verify: a real-Postgres integration test against a known pitcher/season with confirmed `arm_angle` coverage returns a non-null angle, and a pre-2020 pitcher/season returns the "no data" result.
- [x] 1.2 Rewire `PitcherArmSlotEngine.evaluate_arm_slot` (or a new entry point alongside it) to use the real angle/tier from 1.1 as the primary path; keep the existing caller-supplied-coordinates path only as an explicitly labeled what-if/synthetic mode. Verify: `tests/unit/test_arm_slot.py` covers both the real-data path (mocked/fixture rows) and the what-if path, and asserts the what-if result is labeled as such.
- [x] 1.3 Recompute the release-consistency score from real `STDDEV(arm_angle)` (Decision 1) instead of a caller-typed dispersion number; keep its tier cutoffs but mark them `project-derived` in code comment and catalog. Verify: unit test asserts a low-stddev fixture yields a higher consistency score than a high-stddev fixture.
- [x] 1.4 Wire `mlb arm-slot` (`cli.py:3122`) to the real-data path by default (e.g. `--pitcher`/`--season` or matching this project's existing player-lookup CLI conventions), keeping the current hand-typed flags as an explicit what-if flag. Verify: `mlb arm-slot --pitcher <id> --season <year>` against the test database returns a real classification; `--help` output documents both modes.
- [x] 1.5 Update `mlb_baseball/metrics/arm_slot_engine.yaml`: `citation` names Baseball Savant's Arm Angle methodology for the angle/tier; `data_source` names `raw.statcast_pitch.arm_angle`; `status` promoted to `published` (or `validated` if 1.1's test ties out against an independently known value); consistency-score citation stays `project-derived` and the notes field says why. Verify: `mlb_baseball.catalog.load_all()` still validates the entry against the schema.

## 2. `babip_luck_scanner`

- [x] 2.1 Confirm `raw.statcast_pitch.babip_value`'s exact per-row semantics against real rows (design.md Open Questions) — read a sample of real rows for a known batter/date range and compare against that batter's independently known real BABIP for the period. Verify: documented finding (in a code comment or the catalog notes) of what the column actually represents and whether it's safe to use directly.
- [x] 2.2 Add a real-data query helper that computes, for a batter + date range: actual BABIP via the standard definition `(H - HR) / (AB - K - HR + SF)` from `events`/`bb_type`/`description`, and expected BABIP via `AVG(estimated_ba_using_speedangle)` over the same non-HR balls-in-play population. Return an explicit "no data" result for a batter/range with zero qualifying rows. Verify: real-Postgres integration test against a batter/season with known real BABIP; computed actual BABIP matches the independently known value within a stated tolerance.
- [x] 2.3 Rewire `BABIPRegressionEngine.evaluate_babip` (or a new entry point) to use 2.2's real actual/expected BABIP as the primary path, replacing the uncited linear xBABIP formula entirely (Decision 2 — no uncited fallback). Keep the hand-typed CLI inputs only as an explicitly labeled what-if mode. Verify: `tests/unit/test_babip.py` covers the real-data path and the relabeled what-if path.
- [x] 2.4 Wire `mlb babip` (`cli.py:3223`) to the real-data path by default (`--batter`/`--season`-style flags), keeping hand-typed flags as an explicit what-if mode. Verify: `mlb babip --batter <id> --season <year>` against the test database returns a real regression-tier result; `--help` documents both modes.
- [x] 2.5 Update `mlb_baseball/metrics/babip_luck_scanner.yaml`: `citation` names Statcast's Expected Statistics methodology (same source as `statcast_expected_quality_of_contact.yaml`); `data_source` names `raw.statcast_pitch` columns used; `status` promoted per 2.2's tie-out evidence. Verify: catalog schema validation passes.

## 3. `pitch_tunneling_engine`

- [x] 3.1 Do a literature check for Baseball Prospectus's "Introducing Pitch Tunnels" (Long/Pavlidis/Judge, 2017) and any follow-up articles: confirm the tunnel-point definition (~175ms / 23.8 ft already in the docstring) and look for a published source for the elite-tunnel thresholds and score formula. Verify: findings recorded in the catalog entry's notes (either a real citation for the thresholds, or an explicit statement that none was found).
- [x] 3.2 Implement exact tunnel-point trajectory position from each pitch's own `vx0/vy0/vz0`/`ax/ay/az` (closed-form projectile motion, same approach as `vaa.py::pitch_vaa_degrees`), replacing the `poc_factor = 0.30` approximation (Decision 3). Verify: deterministic hand-fixture unit test with known kinematics and an independently hand-calculated expected x/z position at the tunnel point.
- [x] 3.3 Add a real-data query helper that pulls a pitcher's real pitches of two given types over a date range from `raw.statcast_pitch` (release/trajectory columns) and computes the real release distance, tunnel-point separation, and plate-break separation using 3.2. Verify: real-Postgres integration test against a known pitcher/pitch-pair/season returns plausible, non-placeholder values.
- [x] 3.4 Apply 3.1's finding to the tunneling-quality-score/whiff-boost/elite-tunnel-threshold constants: cite them if a real source was found, otherwise keep the current ratio-based formula but label the specific thresholds `project-derived` in code and catalog (Decision 3). Verify: `tests/unit/test_tunnel.py` updated to reflect the real trajectory math and the resolved citation status.
- [x] 3.5 Wire `mlb tunnel` (`cli.py:3443`) to the real-data path by default (`--pitcher`/`--pitch-a`/`--pitch-b`/`--season`-style flags), keeping hand-typed flags as an explicit what-if mode. Verify: CLI invocation against the test database returns a real tunneling evaluation; `--help` documents both modes.
- [x] 3.6 Update `mlb_baseball/metrics/pitch_tunneling_engine.yaml`: `citation`, `data_source`, and `status` updated per 3.1/3.4's findings. Verify: catalog schema validation passes.

## 4. `season_monte_carlo_simulation`

- [x] 4.1 Implement `team_strength_asof(season, as_of, conn)` per design.md Decision 4: aggregate `core.game.home_score`/`away_score` for `game_type = 'regular'`, `game_date < as_of`, per team, feed runs-scored/allowed-per-game into the existing `pythagorean_team_win_pct()`; return the existing `0.500` default (explicitly flagged as a fallback, not computed) for any team with zero qualifying games. Verify: unit test with a synthetic small schedule confirms teams with different real run differentials get different win percentages, and a team with no games gets exactly `0.500` with the fallback flagged.
- [x] 4.2 Add a real-Postgres integration test: for a real past season and a real `as_of` date partway through it, confirm no game on or after `as_of` affects the computed strengths (no-lookahead check per the spec's leakage scenario). Verify: test passes against the disposable test database with real ingested data for that season.
- [x] 4.3 Add `--as-of DATE` to the `season-sim` CLI parser (`cli.py:582`, default: today) and replace the hardcoded `talents = {t: 0.500 for t in season.ALL_MLB_TEAMS}` at `cli.py:1661` with a call to `team_strength_asof(args.season, as_of, conn)`. Verify: `mlb season-sim --season <year>` against the test database produces differentiated `mean_wins` across teams instead of every team clustering at ~81 wins; `--help` documents the new flag.
- [x] 4.4 Update `mlb_baseball/metrics/season_monte_carlo_simulation.yaml`: `data_source` includes `core.game` (runs) in addition to the existing schedule source; `notes` states the CLI no longer uses the flat-0.500 placeholder; `status` promoted if warranted by 4.1/4.2's evidence. Verify: catalog schema validation passes.

## 5. Cross-cutting verification

- [x] 5.1 Run `uv run pytest tests/unit/test_arm_slot.py tests/unit/test_babip.py tests/unit/test_tunnel.py tests/unit/test_season*.py -v` and the new/updated integration tests from tasks 1-4, and record actual pass/fail output.
- [x] 5.2 Run `uv run ruff check mlb_baseball/model/arm_slot.py mlb_baseball/model/babip.py mlb_baseball/model/tunnel.py mlb_baseball/model/season.py mlb_baseball/cli.py` and `uv run mypy mlb_baseball/model/arm_slot.py mlb_baseball/model/babip.py mlb_baseball/model/tunnel.py mlb_baseball/model/season.py`, and record actual output.
- [x] 5.3 Run `mlb_baseball.catalog.load_all()` (or the project's catalog-validation CLI/test) to confirm all four updated YAML entries still validate against the metric-catalog schema, and that no other catalog entry was accidentally touched.
- [x] 5.4 Confirm via `mlb --help` and each command's own `--help` that `arm-slot`/`babip`/`tunnel`/`season-sim` document both their real-data and what-if (or `--as-of`) modes.
- [x] 5.5 Update this change's catalog YAML notes to state which of the two `pitch_tunneling_engine`/`arm_slot_engine` sub-scores ended up cited vs. `project-derived`, so a future reader does not need to re-derive task 1.3/3.1's findings from the diff.
