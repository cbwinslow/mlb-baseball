## 1. Rights check + `backbone` export preset

- [x] 1.1 For each of the ten backbone tables, resolve its publish profile via `mlb_baseball/source_profiles.py` / `export.py`'s allow-list. Verify: a written note in the PR listing each table -> profile -> publishable (yes/no). `player_season` (Baseball-Reference) and `team_season` (Lahman) are the ones to scrutinize.
- [x] 1.2 Add a `backbone` preset to `mlb_baseball/export.py` = the publishable subset of the ten tables (drop any 1.1 flagged ineligible, with its exclusion recorded in the manifest + log). Verify: `uv run pytest tests/integration/test_export*.py -q` plus a new test asserting `mlb export --preset backbone` emits one `data/<table>.parquet` per eligible table + `manifest.json`, and that an ineligible table appears in the manifest's `excluded` list with a reason.
- [x] 1.3 Make the export query `ORDER BY` each table's primary key so row order is deterministic. Verify: a test that runs the export twice and asserts identical row counts and identical first/last row per table.
- [x] 1.4 Add a `dataset_card.md` (as `README.md` in the bundle root) writer: source (Retrosheet event-derived), coverage (seasons present, regular season only), licence, schema version, links to the repo and `docs/RESEARCH.md`. Verify: a test asserting the card exists in the bundle and contains the schema version from the manifest.
- [x] 1.5 Round-trip test: write the bundle with pyarrow, read every Parquet back with `duckdb`, assert each table's column names + types match its `manifest.json` entry. Verify: the test passes.

## 2. Hugging Face publish

- [x] 2.1 Add `huggingface_hub` to the `export` extra. Verify: `uv sync --extra export` succeeds and `python -c "import huggingface_hub"` works.
- [x] 2.2 Add a publish step (`mlb export --preset backbone --publish hf --tag <tag>` or a `publish.py`): `HfApi().upload_folder(<bundle dir>, repo_id=<owner>/mlb-research, repo_type=dataset, revision=<tag>)`, `HF_TOKEN` read from `os.environ` (never a flag, never logged). Verify: a unit test with `huggingface_hub` mocked asserting the folder path, repo_type=dataset, and revision are passed through and that the token is not present in any log record or the command's argv.
- [x] 2.3 Document the manual publish (`HF_TOKEN=... mlb export --preset backbone --publish hf --tag v0.1.0`) in `docs/PUBLIC_API.md` or a delivery doc, with the "never `export HF_TOKEN`" note. Verify: the doc section exists and is linked from `openspec/project.md`.

## 3. `mlb-research` Python package

- [x] 3.1 Create `packages/mlb-research/` with its own `pyproject.toml` (name `mlb-research`, import `mlb_research`, deps `duckdb` + `huggingface_hub` + `pandas`); add it as a `[tool.uv.workspace]` member. Verify: `uv sync` resolves the workspace and `uv run python -c "import mlb_research"` works. (`mlb-research` is also listed under root `dev` extras via `[tool.uv.sources]` workspace-source, so the existing `uv sync --extra dev --extra export` CI/local command installs it with no CI changes -- plain `uv sync --all-packages` also works, per uv's workspace semantics.)
- [x] 3.2 Implement `mlb_research.load(table, *, season=None, version="latest")`: resolve `version` -> HF revision, `hf_hub_download` the table's Parquet (cached), read via `duckdb` with `season` filter pushdown, return a `DataFrame`. Verify: a test using a locally-produced bundle (task 1) served from a `tmp_path` / a mocked `hf_hub_download` -> `load("pitching_season", season=2023)` returns a DataFrame with only 2023 rows and the documented columns.
- [x] 3.3 Error contract: unknown table -> `ValueError` naming it + listing valid names; unreachable version -> a clear wrapped error. Verify: tests for both.
- [x] 3.4 Caching: second `load()` of the same table+version does no network I/O. Verify: a test that patches the downloader to raise on a second call and asserts `load()` still returns.
- [x] 3.5 A `README.md` for the package with the `load()` API and a 5-line example. Verify: the file exists; the example matches the implemented signature.

## 4. DuckDB-WASM query page

- [x] 4.1 `docs/site/query/index.html` + a small JS module: load `@duckdb/duckdb-wasm` from jsDelivr, register the backbone tables as views over their HF Parquet `resolve/<tag>/data/<table>.parquet` URLs, a SQL textarea + Run button + results table, inline error display. Verify: open the file in a browser (or a headless run), submit `SELECT * FROM batting_season WHERE season = 2023 LIMIT 5`, confirm 5 rows render and the network panel shows only HF (no project backend); submit a bad-column query, confirm the DB error text shows in the UI. **Verified with a real headless Chromium (Playwright) run** against the actual `query.js`, pointed at a local CORS-enabled fixture server standing in for `huggingface.co` (confirmed live that HF's `resolve/` endpoint sends the matching `Access-Control-Allow-Origin` header the duckdb-wasm worker, itself loaded from jsDelivr, needs for a cross-origin fetch to succeed) -- the good query rendered a real `<table>` with the correct filtered rows, the bad-column query surfaced DuckDB's own `Binder Error: ...` text in the visible error box, and no other network destination was touched. Caught and fixed two real bugs in the process: (1) the raw `dist/duckdb-browser.mjs` jsDelivr file has an unresolvable bare `"apache-arrow"` import in a browser with no bundler -- switched to jsDelivr's `+esm` transform URL, which resolves it; (2) pinned `@duckdb/duckdb-wasm@1.32.0`, not `latest` -- npm's `latest` dist-tag currently points at a `1.33.1-dev` prerelease, and the newest real tagged release (`v1.33.0`) was never actually published to npm. Cannot verify against the real `cbwinslow/mlb-research` HF dataset itself yet -- it does not exist until task 5.1 (owner-run).
- [x] 4.2 A GitHub Actions `pages` workflow that publishes `docs/site/`. Verify: a successful Pages deployment; the query page is reachable at the Pages URL. Workflow authored (`.github/workflows/pages.yml`, matching GitHub's own build+deploy job split, `contents: read` / `pages: write` + `id-token: write` least-privilege, actions pinned to commit SHA) and YAML-syntax-checked locally; **a real deployment can only be confirmed after this lands on `main`** (the workflow triggers on `push` to `main` under `docs/site/**`) and GitHub Pages is enabled for the repo -- both outside what this session can do from a feature branch. Flagging for the owner to confirm once merged.

## 5. First publish + wire-up + notebook

- [ ] 5.1 **BLOCKED on owner.** Owner runs the first real publish for tag `v0.1.0`. Verify: the HF dataset shows a `v0.1.0` revision with `data/*.parquet`, `manifest.json`, `README.md`, downloadable by tag. Needs the owner's `HF_TOKEN` and a namespace decision (`cbwinslow/mlb-research` vs an org account, design.md Open Questions) -- neither is available to this session. Once decided: `HF_TOKEN=... mlb export --preset backbone --publish hf --tag v0.1.0` (docs/PUBLIC_API.md).
- [ ] 5.2 **BLOCKED on 5.1.** Point the query page URLs and `mlb_research`'s `"latest"` at the published dataset. Verify: `mlb_research.load("batting_season", season=2023)` works from a clean venv with only `pip install mlb-research`; the query page works from its Pages URL. (Today both point at revision `"main"`, the working default -- see `docs/site/query/query.js`'s `HF_REVISION` and `mlb_research`'s `_resolve_revision()`.)
- [ ] 5.3 **BLOCKED on 5.1.** `notebooks/01-<question>.py` (Marimo) answering one real analyst question using only `mlb_research.load(...)`, no DB connection. Verify: `uv run marimo export html notebooks/01-*.py` completes and the notebook makes no psycopg/Postgres call (grep the source). Cannot be verified until a real dataset exists for `load()` to reach -- design.md's own Migration Plan sequences this after the publish (step 6, not step 4/5).
- [x] 5.4 Update `openspec/project.md` (delivery section + milestone progress) and `docs/PUBLIC_API.md`. Verify: links resolve; `openspec validate --all` passes.

## 6. Integration verification

- [x] 6.1 End-to-end: fresh checkout -> `mlb export --preset backbone --out /tmp/b` -> `mlb_research` pointed at `/tmp/b` -> `load()` every backbone table successfully -> the query page (pointed at a local file server for the test) runs a query against each. Verify: a script or documented manual run that exercises all four surfaces against one bundle with no failures. `scripts/verify_delivery_surface.py` -- run for real against a fresh throwaway scratch database (`createdb mlb_verify_scratch`, migrated, seeded, dropped after) in this session: export produced all 8 eligible tables, `mlb_research.load()` returned the correct row count for every one (downloader monkeypatched to the local bundle, no network), and the query page + local bundle were both reachable over HTTP with CORS. The 4th surface (interactive DuckDB-WASM query execution) was separately verified with a real headless-Chromium run during task 4.1 -- not re-driven here since Playwright isn't a project dependency.
