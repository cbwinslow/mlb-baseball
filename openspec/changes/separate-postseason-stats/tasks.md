## 1. Pipeline audit (do first — records the blast radius)

- [x] 1.1 Walk every `gold` builder (`mlb_baseball/sql/*.sql`, `mlb_baseball/report.py`), every SQLMesh model in `transforms/models/`, every materialised view (`SELECT ... FROM pg_class WHERE relkind IN ('v','m')` whose def references game-level data), and every Python aggregation in `mlb_baseball/model/*.py` + `mlb_baseball/*.py`. Record in `openspec/changes/separate-postseason-stats/game-type-audit.md`: per relation — which `game_type`s it includes, where the filter is (file:line) or that it is missing, and the fix. Verify: the audit file lists every game-aggregating relation with a PASS (explicit filter) or GAP (fix noted).
- [x] 1.2 For each GAP found in 1.1, add the explicit `game_type` filter. Verify: re-run the affected builder against a test DB; row counts change only where a postseason game was wrongly included; a targeted integration test covers each fix.
- [x] 1.3 Extend the audit to the **model / ML feature layer**. Done: `game-type-audit.md` gains a "Model / ML feature layer" section. PASS — `features.py` → `game_feature_rebuild.sql` (`game_type = 'regular'` / `ms.game_type = 'R'`); `elo.py`/`age.py`/`experience.py`/`gbm.py`/`stack.py`/`market.py`/`backtest.py`/`drift.py` (inherit `gold.game_feature` / the predicted-game set, both regular-only); `total.py`, `leverage_index.py`, every `model/*.py` reading a `*_retrosheet_update.sql` builder. GAP fixed — `model/season.py::load_schedule_from_db` and `model/ros.py::simulate_ros` (2 queries) read `core.game` for a season's games with **no** `game_type` filter; added `AND g.game_type IN ('regular', 'playoff')` (Game 163 counts). Tests: `tests/integration/test_model_season.py` (a World Series game is excluded from the schedule), `tests/integration/test_model_ros.py` (postseason game not counted in current standings). Noted minor deviation: `game_feature_rebuild.sql`'s `core.game` read uses `= 'regular'` exactly, excluding 4 Game 163 team-seasons (conservative direction — follow-up, not a contamination bug).
      - **DB views** also audited (`pg_class` `relkind IN ('v','m')`, all of `gold`/`core`/`serve`/`meta`). GAP: `serve.ros_team_standings` counted every completed game (regular + postseason + spring + exhibition) — 2023 TEX showed 210 GP / 116 W vs the real 162 / 90. Fixed in `migrations/0101_ros_team_standings_regular_season.sql` (`game_type IN ('regular','playoff')` on the `completed` CTE) + `serve.health_check()` gains a "games_played ≤ 163" guard. Every other `serve.*` view is built on `gold.game_feature` / `gold.prediction` / `gold.*_season` (regular-only) — PASS.

## 2. Fix the raw Baseball-Reference ingest

- [x] 2.1 In `mlb_baseball/connectors/bref.py`, replace the `pybaseball.batting_stats_bref` / `pitching_stats_bref` calls with `pybaseball.batting_stats_range` / `pitching_stats_range` over `f"{season}-03-15"` → a regular-season end date. Build a per-season `_REGULAR_SEASON_END` map (default `f"{season}-10-01"`; explicit entries for every season 2008-2026 from the actual last regular-season game date / any tiebreaker — sourced, not guessed). Verify: a unit test with pybaseball mocked asserts the date range passed excludes November for every season; `basedpyright` / `ruff` clean.
      - Done: `_SEASON_START` / `_REGULAR_SEASON_END` (explicit 2008–2025, sourced from `core.game` `game_type IN ('regular','playoff')` incl. pre-2022 Game 163 tiebreakers; 2026 → `_DEFAULT_END`), `_season_window()`, `_load_table` now calls `call_with_retry(fn, start_dt, end_dt)`. `tests/unit/test_bref_window.py` (window never reaches Nov, every completed season has a sourced end date). `tests/integration/test_bref_load.py` fakes updated to `(start_dt, end_dt)`. `ruff` + `mypy` clean; 73 bref tests pass. Docstring + `bref.py.dox.md` + `docs/DATA_SOURCES.md` updated.
- [x] 2.2 Re-ingest `raw.bref_batting` / `raw.bref_pitching` — **2021–2026 only** (2008–2019 are already regular-season-only, ADR-282; 2020 is the COVID season). Two small tables, one row per player per season, ~12 HTTP requests. Owner-run; commands in `reingest-runbook.md`.
      - **Done on prod 2026-09-07** (run in-session with owner authorization). Deleted 4,386 batting + 5,205 pitching rows for `_season >= 2021`, purged the stale/corrupt pybaseball cache, re-ran `mlb ingest bref --mode bootstrap`. Verified: Marcus Semien 2023 = **162 G / 753 PA** (was 179 / 835); Corey Seager 2023 = 119 G, Freddie Freeman 2021 = 159 G, José Altuve 2022 = 141 G; `max(g::int)` per season 2021-2025 = 162/162/162/162/163 (2025 Game 163), `max(pa::int)` = 753. Runbook column names corrected (`g`/`pa` lowercase text, not `"G"`/`"PA"`) and a `mlb migrate` step added.

## 3. Rebuild `gold.player_season` / `gold.team_season` clean

- [x] 3.1 Run `mlb report` against the re-ingested raw. Verify: `SELECT max(games) FROM gold.player_season` ≤ 163; the `verify_baseball_reference_tie_out.py` bulk cross-check passes with its ceiling at 2025 for a spot run.
      - **Done on prod 2026-09-07.** First two `mlb report` runs surfaced the 4.2 float-text bug (relation "gold.batting_postseason" does not exist → then InvalidTextRepresentation); third run clean: `gold.player_season` 30,762 rows, `gold.batting_postseason` 36,948, `gold.pitching_postseason` 14,844. `gold.player_season` max games ≤ 163 (2025 Game 163), 0 rows over `games > 163 OR pa > 800`. `scripts/verify_baseball_reference_tie_out.py` **passes**: Aaron Judge 2022 (11 fields) + Gerrit Cole 2023 (17 fields) exact vs Baseball-Reference; bulk 2008-2025 event-backbone-vs-player_season cross-check within tolerance on ≤2% of player-seasons every field.

## 4. New postseason relations (player-grain; the Lahman `BattingPost` parallel)

- [x] 4.1 `migrations/0100_gold_postseason.sql` — `gold.batting_postseason` / `gold.pitching_postseason`, column shape mirrors `gold.batting_season` / `gold.pitching_season` plus `sb`/`cs` (batting), a real `era` (pitching — Lahman carries ER), `round text`, `is_combined`, `is_career`. CHECK constraints tie the flags to `season`/`round`/`team_id` nullability; three partial unique indexes (per-round, combined, career). Applies in the pytest migrate; `test_report_postseason.py` exercises the constraints.
- [x] 4.2 `mlb_baseball/sql/batting_postseason_build.sql` / `pitching_postseason_build.sql` — from `raw.lahman_batting_post` / `raw.lahman_pitching_post`; `playerid` → `core.player.bbref_id` with a `raw.lahman_people.retroid` → `core.player.retro_id` fallback (~99.9% resolve, confirmed vs prod); `teamid`+`yearid` → `core.team` via `raw.lahman_teams`; `round` kept verbatim; per-round + `is_combined` + `is_career` rows. Wired into `report.run()` via `_build_backbone_relation` (source-presence check → skips cleanly without Lahman postseason). `report.health_check()` gains per-round join-coverage (tolerance 0) + combined/career coverage checks; unresolved ids surface as a coverage shortfall, not a silent drop. `tests/integration/test_report_postseason.py`: per-round/combined/career totals hand-checked, retro-id fallback, idempotency.
      - **Owner-run rebuild found + fixed a bug (2026-09-07).** Real `raw.lahman_*_post` columns are text and pandas float-formats every nullable-int column (`"6.0"` not `"6"`) — ~18k rows each for `so`/`ibb`/`hbp`/`sf`/`sh`/`cs`/`gidp`. The build cast `nullif(x,'')::integer`, which raises `InvalidTextRepresentation` on `"6.0"`, so **no** postseason relation was built. Fixed: `::numeric::integer` in both build SQLs (+ header note). New test `test_report_postseason.py::test_float_formatted_source_counts_are_parsed`, red-green verified. `mlb report` now builds `gold.batting_postseason` (36,948 rows) / `gold.pitching_postseason` (14,844) clean on prod.
- [ ] 4.3 Reconciliation: a `mlb doctor` check comparing the Lahman-built postseason season totals against a Retrosheet-event rebuild (`raw.retrosheet_event` where `game_type` is a postseason type) for the modern era, calibrated like `starter.py`'s regular-season reconciliation. Verify: the check runs and passes against real data within a documented tolerance.
      - **Deferred to a follow-up.** This is a cross-check, not a build input (design D2). Calibrating it needs the real Retrosheet postseason event data + real Lahman postseason totals (prod), so its verification is owner-run regardless; the primary guards (5.1) are in place and the tie-out gate re-covers 2021+ once the re-ingest lands. Tracked as an OpenSpec follow-up.

## 5. Guards

- [x] 5.1 `mlb doctor` checks in `report.health_check()` (design.md D4): (1) regular-season envelope — fails on any `gold.player_season` row `games > 163 OR pa > 800`, any `gold.team_season` row over its per-era decision ceiling, plus the same on `gold.batting_season` / `gold.pitching_season`; (2) postseason-relation round sanity — every non-null `round` matches `_PS_ROUND_RE` (WS / NWS / CS / NNC / NSC / league-prefixed C·CS·DS·WC·DIV·P). The Retrosheet-event overlap check is folded into deferred 4.3. Round-sanity passes in `test_report_postseason.py`.
      - **Owner-run rebuild found + fixed a bug (2026-09-07).** The envelope check used a flat `> 163`, which flags **correct** pre-1969 data as contamination — indistinguishable from a real leak: 1962 SF Giants 103-62 (165 decisions, best-of-3 pennant playoff) in `gold.team_season`; Billy Williams / Ron Santo 1965 + César Tovar 1967 at 164 G (1960s tie-game replays counted) in `gold.batting_season`. Fixed: era-scope both to `> CASE WHEN season < 1969 THEN 165 ELSE 163 END` (a leaked postseason series adds far more than 2 games, so real contamination is still caught); `pa > 800` kept universal (season record ~778). New test `test_report.py::test_envelope_check_is_era_scoped_for_pre_1969_tiebreakers_and_tie_replays`. Verified against prod: 0 rows over envelope for player / team / batting / pitching season.

## 6. Reconciliation + export + docs

- [x] 6.1 `mlb_baseball/model/starter.py` docstrings updated — the postseason-mix cause is marked fixed in `separate-postseason-stats`; the note says the outs / strikeout tolerances can be re-measured and tightened once the owner re-ingests `raw.bref_*` (the ~98.3% Retrosheet coverage gap is the only remaining reason for a non-zero tolerance, so the numbers can't safely change until that data exists). `bullpen.py` has **no** bref reconciliation tolerance — its check is internal (`relief + starter outs = team total outs`, tolerance 0, already regular-season-scoped) — nothing to tighten. `scripts/verify_baseball_reference_tie_out.py` cap raised 2019 → 2025 (2020 COVID still skipped).
- [x] 6.2 `mlb_baseball/export.py` — `gold.batting_postseason` / `gold.pitching_postseason` allow-list entries (`local_research`). `mlb export` lists them; `test_export.py` / `test_export_integration.py` pass.
- [x] 6.3 Docs: `docs/DECISIONS.md` (ADR-282 → RESOLVED; new **ADR-283** — baseball.computer is the game-type reference, regular-season-only aggregates everywhere incl. views / `mlb doctor` metrics / model+ML features), `docs/RESEARCH.md` (`raw.bref_pitching` now regular-season only), `docs/DATA_DICTIONARY.md` (§ 3.10 new postseason relations; regular-season-only statement + envelope check on the season backbone), `docs/TABLE_CONTRACTS.md` (`gold.player_season` / `_season` rows + new postseason rows), `docs/KNOWLEDGE_BASE.md` (`seed_game_types` mapping), `openspec/project.md` NOW block. `openspec validate separate-postseason-stats --strict` passes.

## 7. Verification

- [x] 7.1 Fresh-checkout smoke: confirm no postseason game lands in any regular-season relation and the postseason relations are populated only by postseason games.
      - **Done on prod 2026-09-07** via the full `reingest-runbook.md` pass (migrate → re-ingest → report). `report.health_check()` run directly against production `mlb`: **33/33 checks pass** — every join-coverage check at 100%, both regular-season envelope checks clean (0 rows over), round-sanity clean, `gold.batting_postseason` (36,948) / `gold.pitching_postseason` (14,844) fully covered (18,672 / 7,472 per-round rows, 18,276 combined+career). The postseason source tables are 100% postseason by construction (Lahman BattingPost / PitchingPost).
- [x] 7.2 `openspec validate --all` and `openspec validate separate-postseason-stats --strict` exit 0. `ruff` + `sqlfluff` + `mypy` clean across every touched file. Targeted tests pass: `test_bref_window.py` + `test_bref_load.py` (73), `test_model_season.py` / `test_model_ros.py`, `test_export*` / `test_health`, `test_report_{batting,pitching}_{game,season}.py` + `test_report_career.py`, `test_doctor.py` + `test_serve.py`.
      - **Re-verified after the 2026-09-07 owner-run fixes.** New / changed since the prior 7.2 run: `batting_postseason_build.sql` / `pitching_postseason_build.sql` (`::numeric::integer`), `report.py` (era-scoped envelope checks), `test_report_postseason.py` (+`test_float_formatted_source_counts_are_parsed`), `test_report.py` (+`test_envelope_check_is_era_scoped_for_pre_1969_tiebreakers_and_tie_replays`). `ruff` + `ruff format --check` + `mypy mlb_baseball/report.py` + `sqlfluff` clean. `test_report_postseason.py` 4/4 pass; the new era-scope test passes and is red-green verified (revert `report.py` → it fails at `assert _check_ok`, restore → passes). `report.health_check()` run against production `mlb`: 33/33 pass.
